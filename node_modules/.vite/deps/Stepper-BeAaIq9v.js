import { r as __toESM } from "./rolldown-runtime-DC62tzP2.js";
import { t as require_react } from "./react.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { A as generateUtilityClass, Z as require_prop_types, g as useRtl, j as clsx, k as generateUtilityClasses } from "./createTheme-BiXad5I9.js";
import { n as styled, o as composeClasses, t as useDefaultProps } from "./DefaultPropsProvider-BQBI6Ghv.js";
import { t as memoTheme } from "./memoTheme-DgNLaLe1.js";
import { i as RovingTabIndexContext, n as useRovingTabIndexItem, o as isMuiElement_default, r as useRovingTabIndexRoot } from "./useRovingTabIndex-BQu09KYu.js";
import { t as integerPropType } from "./integerPropType-DfN_STxq.js";
import { n as buttonBaseClasses, t as ButtonBase } from "./ButtonBase-BLh17MsU.js";
import { i as useStepperContext, r as StepperContext, t as StepContext } from "./StepContext-DBGuHtBo.js";
import { r as stepLabelClasses, t as StepLabel } from "./StepLabel-D1RdVR4U.js";
//#region node_modules/@mui/material/StepButton/stepButtonClasses.mjs
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var import_prop_types = /* @__PURE__ */ __toESM(require_prop_types(), 1);
function getStepButtonUtilityClass(slot) {
	return generateUtilityClass("MuiStepButton", slot);
}
var stepButtonClasses = generateUtilityClasses("MuiStepButton", [
	"root",
	"horizontal",
	"vertical",
	"touchRipple"
]);
//#endregion
//#region node_modules/@mui/material/StepButton/StepButton.mjs
var import_jsx_runtime = require_jsx_runtime();
var useUtilityClasses$2 = (ownerState) => {
	const { classes, orientation } = ownerState;
	return composeClasses({
		root: ["root", orientation],
		touchRipple: ["touchRipple"]
	}, getStepButtonUtilityClass, classes);
};
var StepButtonRoot = styled(ButtonBase, {
	name: "MuiStepButton",
	slot: "Root",
	overridesResolver: (props, styles) => {
		const { ownerState } = props;
		return [
			{ [`& .${stepButtonClasses.touchRipple}`]: styles.touchRipple },
			styles.root,
			styles[ownerState.orientation]
		];
	}
})(memoTheme(({ theme }) => ({
	width: "100%",
	padding: "24px 16px",
	margin: "-24px -16px",
	boxSizing: "content-box",
	[`& .${stepButtonClasses.touchRipple}`]: {
		color: "rgba(0, 0, 0, 0.3)",
		...theme.applyStyles("dark", { color: "rgba(255, 255, 255, 0.3)" })
	},
	...theme.focusVisible && { [`&.${buttonBaseClasses.focusVisible} .${stepLabelClasses.root}`]: {
		borderRadius: (theme.vars || theme).shape.borderRadius,
		...theme.focusVisible
	} },
	variants: [{
		props: { orientation: "vertical" },
		style: {
			justifyContent: "flex-start",
			padding: "8px",
			margin: "-8px"
		}
	}]
})));
var RovingStepButton = /*#__PURE__*/ import_react.forwardRef(function RovingStepButton(props, ref) {
	const { children, disabled, index, ...other } = props;
	const rovingItemProps = useRovingTabIndexItem({
		id: index,
		ref,
		disabled
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StepButtonRoot, {
		disabled,
		...rovingItemProps,
		...other,
		children
	});
});
var StepButton = /*#__PURE__*/ import_react.forwardRef(function StepButton(inProps, ref) {
	const props = useDefaultProps({
		props: inProps,
		name: "MuiStepButton"
	});
	const { children, className, icon, optional, ...other } = props;
	const { disabled, active, index } = import_react.useContext(StepContext);
	const { orientation, totalSteps, isTabList } = useStepperContext();
	const ownerState = {
		...props,
		orientation
	};
	const classes = useUtilityClasses$2(ownerState);
	const childProps = {
		icon,
		optional
	};
	const child = isMuiElement_default(children, ["StepLabel"]) ? /*#__PURE__*/ import_react.cloneElement(children, childProps) : /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StepLabel, {
		...childProps,
		children
	});
	const stepButtonProps = {
		internalNativeButton: true,
		focusRipple: true,
		disabled,
		TouchRippleProps: { className: classes.touchRipple },
		className: clsx(classes.root, className),
		ownerState,
		"aria-selected": active,
		"aria-posinset": index + 1,
		"aria-setsize": totalSteps,
		role: "tab",
		internalDisabledThemeFocusVisible: true,
		...other
	};
	if (isTabList) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(RovingStepButton, {
		...stepButtonProps,
		index,
		ref,
		children: child
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StepButtonRoot, {
		ref,
		tabIndex: active ? 0 : -1,
		...stepButtonProps,
		children: child
	});
});
StepButton.propTypes = {
	/**
	* Can be a `StepLabel` or a node to place inside `StepLabel` as children.
	*/
	children: import_prop_types.default.node,
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* @ignore
	*/
	className: import_prop_types.default.string,
	/**
	* The icon displayed by the step label.
	*/
	icon: import_prop_types.default.node,
	/**
	* The optional node to display.
	*/
	optional: import_prop_types.default.node,
	/**
	* The system prop that allows defining system overrides as well as additional CSS styles.
	*/
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	])
};
//#endregion
//#region node_modules/@mui/material/StepConnector/stepConnectorClasses.mjs
function getStepConnectorUtilityClass(slot) {
	return generateUtilityClass("MuiStepConnector", slot);
}
var stepConnectorClasses = generateUtilityClasses("MuiStepConnector", [
	"root",
	"horizontal",
	"vertical",
	"alternativeLabel",
	"active",
	"completed",
	"disabled",
	"line"
]);
//#endregion
//#region node_modules/@mui/material/StepConnector/StepConnector.mjs
var useUtilityClasses$1 = (ownerState) => {
	const { classes, orientation, alternativeLabel, active, completed, disabled } = ownerState;
	return composeClasses({
		root: [
			"root",
			orientation,
			alternativeLabel && "alternativeLabel",
			active && "active",
			completed && "completed",
			disabled && "disabled"
		],
		line: ["line"]
	}, getStepConnectorUtilityClass, classes);
};
var StepConnectorRoot = styled("div", {
	name: "MuiStepConnector",
	slot: "Root",
	overridesResolver: (props, styles) => {
		const { ownerState } = props;
		return [
			styles.root,
			styles[ownerState.orientation],
			ownerState.alternativeLabel && styles.alternativeLabel,
			ownerState.completed && styles.completed
		];
	}
})({
	flex: "1 1 auto",
	variants: [
		{
			props: { orientation: "vertical" },
			style: { marginLeft: 12 }
		},
		{
			props: {
				orientation: "vertical",
				alternativeLabel: true
			},
			style: {
				marginLeft: "auto",
				marginRight: 12
			}
		},
		{
			props: {
				orientation: "horizontal",
				alternativeLabel: true
			},
			style: {
				position: "absolute",
				top: 12,
				left: "calc(-50% + 20px)",
				right: "calc(50% + 20px)"
			}
		}
	]
});
var StepConnectorLine = styled("span", {
	name: "MuiStepConnector",
	slot: "Line"
})(memoTheme(({ theme }) => {
	const borderColor = theme.palette.mode === "light" ? theme.palette.grey[400] : theme.palette.grey[600];
	return {
		display: "block",
		borderColor: theme.vars ? theme.vars.palette.StepConnector.border : borderColor,
		variants: [{
			props: { orientation: "horizontal" },
			style: {
				borderTopStyle: "solid",
				borderTopWidth: 1
			}
		}, {
			props: { orientation: "vertical" },
			style: {
				borderLeftStyle: "solid",
				borderLeftWidth: 1,
				minHeight: 24
			}
		}]
	};
}));
var StepConnector = /*#__PURE__*/ import_react.forwardRef(function StepConnector(inProps, ref) {
	const props = useDefaultProps({
		props: inProps,
		name: "MuiStepConnector"
	});
	const { className, ...other } = props;
	const { alternativeLabel, orientation = "horizontal" } = useStepperContext();
	const { active, disabled, completed } = import_react.useContext(StepContext);
	const ownerState = {
		...props,
		alternativeLabel,
		orientation,
		active,
		completed,
		disabled
	};
	const classes = useUtilityClasses$1(ownerState);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StepConnectorRoot, {
		className: clsx(classes.root, className),
		ref,
		ownerState,
		...other,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StepConnectorLine, {
			className: classes.line,
			ownerState
		})
	});
});
StepConnector.propTypes = {
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* @ignore
	*/
	className: import_prop_types.default.string,
	/**
	* The system prop that allows defining system overrides as well as additional CSS styles.
	*/
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	])
};
//#endregion
//#region node_modules/@mui/material/Stepper/stepperClasses.mjs
function getStepperUtilityClass(slot) {
	return generateUtilityClass("MuiStepper", slot);
}
var stepperClasses = generateUtilityClasses("MuiStepper", [
	"root",
	"horizontal",
	"vertical",
	"nonLinear",
	"alternativeLabel"
]);
//#endregion
//#region node_modules/@mui/material/Stepper/Stepper.mjs
var useUtilityClasses = (ownerState) => {
	const { orientation, nonLinear, alternativeLabel, classes } = ownerState;
	return composeClasses({ root: [
		"root",
		orientation,
		nonLinear && "nonLinear",
		alternativeLabel && "alternativeLabel"
	] }, getStepperUtilityClass, classes);
};
var StepperRoot = styled("ol", {
	name: "MuiStepper",
	slot: "Root",
	overridesResolver: (props, styles) => {
		const { ownerState } = props;
		return [
			styles.root,
			styles[ownerState.orientation],
			ownerState.alternativeLabel && styles.alternativeLabel,
			ownerState.nonLinear && styles.nonLinear
		];
	}
})({
	display: "flex",
	listStyle: "none",
	margin: 0,
	padding: 0,
	variants: [
		{
			props: { orientation: "horizontal" },
			style: {
				flexDirection: "row",
				alignItems: "center"
			}
		},
		{
			props: {
				orientation: "horizontal",
				alternativeLabel: false
			},
			style: { gap: 8 }
		},
		{
			props: { orientation: "vertical" },
			style: { flexDirection: "column" }
		},
		{
			props: { alternativeLabel: true },
			style: { alignItems: "flex-start" }
		},
		{
			props: {
				orientation: "vertical",
				alternativeLabel: true
			},
			style: { alignItems: "flex-end" }
		}
	]
});
var defaultConnector = /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StepConnector, {});
function RovingStepper(props) {
	const { children, className, component, forwardedRef, isRtl, orientation, ownerState, ...other } = props;
	const rovingContainer = useRovingTabIndexRoot({
		orientation,
		isRtl
	});
	const rovingContainerProps = rovingContainer.getContainerProps(forwardedRef, other.onFocus, other.onKeyDown);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(RovingTabIndexContext.Provider, {
		value: rovingContainer,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StepperRoot, {
			as: component,
			ownerState,
			className,
			role: "tablist",
			"aria-orientation": orientation,
			...other,
			...rovingContainerProps,
			children
		})
	});
}
var Stepper = /*#__PURE__*/ import_react.forwardRef(function Stepper(inProps, ref) {
	const isRtl = useRtl();
	const props = useDefaultProps({
		props: inProps,
		name: "MuiStepper"
	});
	const { activeStep = 0, alternativeLabel = false, children, className, component = "ol", connector = defaultConnector, nonLinear = false, orientation = "horizontal", ...other } = props;
	const ownerState = {
		...props,
		nonLinear,
		alternativeLabel,
		orientation,
		component
	};
	const classes = useUtilityClasses(ownerState);
	const childrenArray = import_react.Children.toArray(children).filter(Boolean);
	const totalSteps = childrenArray.length;
	const isTabList = childrenArray.some((child) => {
		if (!/*#__PURE__*/ import_react.isValidElement(child)) return false;
		if (child.type === StepButton) return true;
		const grandChildren = child.props.children;
		if (grandChildren) return import_react.Children.toArray(grandChildren).some((grandChild) => /*#__PURE__*/ import_react.isValidElement(grandChild) && grandChild.type === StepButton);
		return false;
	});
	const steps = childrenArray.map((step, index) => {
		return /*#__PURE__*/ import_react.cloneElement(step, {
			index,
			last: index + 1 === totalSteps,
			...step.props
		});
	});
	const contextValue = import_react.useMemo(() => ({
		activeStep,
		alternativeLabel,
		connector,
		nonLinear,
		orientation,
		totalSteps,
		isTabList
	}), [
		activeStep,
		alternativeLabel,
		connector,
		nonLinear,
		orientation,
		totalSteps,
		isTabList
	]);
	if (!isTabList) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StepperContext.Provider, {
		value: contextValue,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StepperRoot, {
			as: component,
			ownerState,
			className: clsx(classes.root, className),
			ref,
			...other,
			children: steps
		})
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StepperContext.Provider, {
		value: contextValue,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(RovingStepper, {
			forwardedRef: ref,
			isRtl,
			className: clsx(classes.root, className),
			component,
			orientation,
			ownerState,
			...other,
			children: steps
		})
	});
});
Stepper.propTypes = {
	/**
	* Set the active step (zero based index).
	* Set to -1 to disable all the steps.
	* @default 0
	*/
	activeStep: integerPropType,
	/**
	* If set to 'true' and orientation is horizontal,
	* then the step label will be positioned under the icon.
	* If set to 'true' and orientation is vertical,
	* it reverses the position of the label and content.
	* @default false
	*/
	alternativeLabel: import_prop_types.default.bool,
	/**
	* Two or more `<Step />` components.
	*/
	children: import_prop_types.default.node,
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* @ignore
	*/
	className: import_prop_types.default.string,
	/**
	* The component used for the root node.
	* Either a string to use a HTML element or a component.
	*/
	component: import_prop_types.default.elementType,
	/**
	* An element to be placed between each step.
	* @default <StepConnector />
	*/
	connector: import_prop_types.default.element,
	/**
	* If set the `Stepper` will not assist in controlling steps for linear flow.
	* @default false
	*/
	nonLinear: import_prop_types.default.bool,
	/**
	* The component orientation (layout flow direction).
	* @default 'horizontal'
	*/
	orientation: import_prop_types.default.oneOf(["horizontal", "vertical"]),
	/**
	* The system prop that allows defining system overrides as well as additional CSS styles.
	*/
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	])
};
//#endregion
export { getStepConnectorUtilityClass as a, getStepButtonUtilityClass as c, StepConnector as i, stepButtonClasses as l, getStepperUtilityClass as n, stepConnectorClasses as o, stepperClasses as r, StepButton as s, Stepper as t };
