import { r as __toESM } from "./rolldown-runtime-DC62tzP2.js";
import { t as require_react } from "./react.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { A as generateUtilityClass, Z as require_prop_types, j as clsx, k as generateUtilityClasses } from "./createTheme-BiXad5I9.js";
import { n as styled, o as composeClasses, t as useDefaultProps } from "./DefaultPropsProvider-BQBI6Ghv.js";
import { t as integerPropType } from "./integerPropType-DfN_STxq.js";
import { i as useStepperContext, t as StepContext } from "./StepContext-DBGuHtBo.js";
//#region node_modules/@mui/material/Step/stepClasses.mjs
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var import_prop_types = /* @__PURE__ */ __toESM(require_prop_types(), 1);
function getStepUtilityClass(slot) {
	return generateUtilityClass("MuiStep", slot);
}
var stepClasses = generateUtilityClasses("MuiStep", [
	"root",
	"horizontal",
	"vertical",
	"alternativeLabel",
	"completed"
]);
//#endregion
//#region node_modules/@mui/material/Step/Step.mjs
var import_jsx_runtime = require_jsx_runtime();
var useUtilityClasses = (ownerState) => {
	const { classes, orientation, alternativeLabel, completed } = ownerState;
	return composeClasses({ root: [
		"root",
		orientation,
		alternativeLabel && "alternativeLabel",
		completed && "completed"
	] }, getStepUtilityClass, classes);
};
var StepRoot = styled("li", {
	name: "MuiStep",
	slot: "Root",
	overridesResolver: (props, styles) => {
		const { ownerState } = props;
		return [
			styles.root,
			styles[ownerState.orientation],
			ownerState.alternativeLabel && styles.alternativeLabel,
			ownerState.completed && styles.completed
		];
	}
})({ variants: [
	{
		props: {
			orientation: "horizontal",
			alternativeLabel: false,
			hasConnector: false
		},
		style: { paddingLeft: 8 }
	},
	{
		props: {
			orientation: "horizontal",
			alternativeLabel: false,
			last: true
		},
		style: { paddingRight: 8 }
	},
	{
		props: {
			orientation: "horizontal",
			alternativeLabel: false,
			hasConnector: true
		},
		style: {
			flex: "1 1 auto",
			display: "grid",
			gridTemplateColumns: "1fr auto",
			alignItems: "center",
			gap: 8
		}
	},
	{
		props: {
			orientation: "vertical",
			alternativeLabel: true
		},
		style: {
			display: "flex",
			flexDirection: "column"
		}
	},
	{
		props: {
			orientation: "horizontal",
			alternativeLabel: true
		},
		style: {
			flex: 1,
			position: "relative"
		}
	}
] });
var Step = /*#__PURE__*/ import_react.forwardRef(function Step(inProps, ref) {
	const props = useDefaultProps({
		props: inProps,
		name: "MuiStep"
	});
	const { active: activeProp, children, className, component = "li", completed: completedProp, disabled: disabledProp, expanded = false, index, last, ...other } = props;
	const { activeStep, connector, alternativeLabel, orientation, nonLinear, isTabList } = useStepperContext();
	let [active = false, completed = false, disabled = false] = [
		activeProp,
		completedProp,
		disabledProp
	];
	if (activeStep === index) active = activeProp !== void 0 ? activeProp : true;
	else if (!nonLinear && activeStep > index) completed = completedProp !== void 0 ? completedProp : true;
	else if (!nonLinear && activeStep < index) disabled = disabledProp !== void 0 ? disabledProp : true;
	const contextValue = import_react.useMemo(() => ({
		index,
		last,
		expanded,
		icon: index + 1,
		active,
		completed,
		disabled
	}), [
		index,
		last,
		expanded,
		active,
		completed,
		disabled
	]);
	const hasConnector = !!connector && index !== 0;
	const ownerState = {
		...props,
		active,
		orientation,
		alternativeLabel,
		completed,
		disabled,
		expanded,
		component,
		hasConnector
	};
	const classes = useUtilityClasses(ownerState);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StepContext.Provider, {
		value: contextValue,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(StepRoot, {
			as: component,
			className: clsx(classes.root, className),
			ref,
			ownerState,
			role: isTabList ? "presentation" : void 0,
			...other,
			children: [hasConnector ? connector : null, children]
		})
	});
});
Step.propTypes = {
	/**
	* Sets the step as active. Is passed to child components.
	*/
	active: import_prop_types.default.bool,
	/**
	* Should be `Step` sub-components such as `StepLabel`, `StepContent`.
	*/
	children: import_prop_types.default.node,
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* @ignore
	*/
	className: import_prop_types.default.string,
	/**
	* Mark the step as completed. Is passed to child components.
	*/
	completed: import_prop_types.default.bool,
	/**
	* The component used for the root node.
	* Either a string to use a HTML element or a component.
	*/
	component: import_prop_types.default.elementType,
	/**
	* If `true`, the step is disabled, will also disable the button if
	* `StepButton` is a child of `Step`. Is passed to child components.
	*/
	disabled: import_prop_types.default.bool,
	/**
	* Expand the step.
	* @default false
	*/
	expanded: import_prop_types.default.bool,
	/**
	* The position of the step.
	* The prop defaults to the value inherited from the parent Stepper component.
	*/
	index: integerPropType,
	/**
	* If `true`, the Step is displayed as rendered last.
	* The prop defaults to the value inherited from the parent Stepper component.
	*/
	last: import_prop_types.default.bool,
	/**
	* The system prop that allows defining system overrides as well as additional CSS styles.
	*/
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	])
};
//#endregion
export { getStepUtilityClass as n, stepClasses as r, Step as t };
